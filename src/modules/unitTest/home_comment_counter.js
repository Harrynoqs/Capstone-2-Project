/* eslint-disable */
export const homeItmes = (array) => {
    if(array.length){
        return array.length
    } else {
       return "0"
    }
  }

export const commentCounter = (array) => {
    if(array.length){
        return array.length
    } else {
       return "0"
    }
    
}
